name: Sync Upcoming Contests

on:
  schedule:
    - cron: "13 5 * * *" # 5:13am IST

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Daily cron job
        run: |
          curl --request POST \
          --url 'https://events-tutly.vercel.app/cron/sync-upcomming-contests/' 
